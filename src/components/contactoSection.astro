<section>
    <div class="titleContainer">
        <h2 class="title">Contactanos</h2>
    </div>
    <article>
        <div class="WhatsApp">
            <div class="apptitle">
                <img src="/whatsapp.png" alt="WhatsApp" />
                <h2>WhatsApp</h2>
            </div>
            <span>Contáctanos directamente para una respuesta rápida.</span>
            <a
                class="btnWhat"
                href="https://wa.me/51997002244?text=Hola%20vi%20tu%20página%20y%20quisiera%20hacer%20una%20consulta%20sobre%20los%20pedidos"
                target="_blank"
                rel="noopener noreferrer"
            >
                997 002 244
            </a>
        </div>
        <div class="Facebook">
            <div class="facetitle">
                <img src="/Facebook.png" alt="Facebook" />
                <h2>Facebook</h2>
            </div>
            <span>Síguenos para ver nuestras creaciones y promociones.</span>
            <a
                class="btnFace"
                href="https://www.facebook.com/profile.php?id=61560020993338"
                target="_blank"
            >
                PassionLove
            </a>
        </div>
    </article>
</section>

<style>
    @import url("https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,100;0,300;0,400;0,600;0,700;1,400&display=swap");

    * {
        box-sizing: border-box;
    }

    section {
        display: grid;
        grid-template-columns: 1fr;
        min-height: 70vh;
        padding: 2rem;
        gap: 2rem;
    }
    .titleContainer {
        margin-bottom: 20px;
    }
    div {
        display: flex;
        justify-content: center;
    }

    .title {
        font-family: "Instrument Sans";
        color: #ec4960;
        font-size: 60px;
        margin: 0;
        text-align: center;
    }

    article {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
        align-items: start;
    }

    .WhatsApp,
    .Facebook {
        font-family: "Instrument Sans";
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        padding: 2rem;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(10px);
        transition: transform 0.3s ease;
    }

    .WhatsApp:hover,
    .Facebook:hover {
        transform: translateY(-10px);
    }

    .WhatsApp h2 {
        color: #29a71a;
        font-size: 40px;
        margin: 0;
    }

    .Facebook h2 {
        color: #1976d2;
        font-size: 40px;
        margin: 0;
    }

    .apptitle,
    .facetitle {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-bottom: 1.5rem;
    }

    img {
        height: 70px;
        width: 70px;
        object-fit: contain;
    }

    .WhatsApp span,
    .Facebook span {
        font-size: 25px;
        max-width: 350px;
        line-height: 1.4;
        margin-bottom: 2rem;
        color: #666;
    }

    .btnWhat,
    .btnFace {
        font-size: 27px;
        color: white;
        border: none;
        height: 70px;
        width: 200px;
        border-radius: 20px;
        text-align: center;
        text-decoration: none;
        display: flex;
        justify-content: center;
        align-items: center;
        transition: all 0.3s ease;
        font-weight: 600;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
    }

    .btnWhat {
        background-color: #29a71a;
    }

    .btnFace {
        background-color: #1976d2;
    }

    .btnWhat:hover {
        background-color: #22871a;
        transform: scale(1.05) translateY(-2px);
        box-shadow: 0 8px 25px rgba(41, 167, 26, 0.4);
    }

    .btnFace:hover {
        background-color: #1565c0;
        transform: scale(1.05) translateY(-2px);
        box-shadow: 0 8px 25px rgba(25, 118, 210, 0.4);
    }

    /* 🔹 TABLET (992px - 769px) */
    @media (max-width: 992px) {
        section {
            padding: 1.5rem;
        }

        .title {
            font-size: 50px;
            margin-bottom: 100px;
        }

        .WhatsApp h2,
        .Facebook h2 {
            font-size: 35px;
        }

        .WhatsApp span,
        .Facebook span {
            font-size: 22px;
            max-width: 300px;
        }

        img {
            height: 60px;
            width: 60px;
        }

        .btnWhat,
        .btnFace {
            font-size: 24px;
            height: 65px;
            width: 180px;
        }

        article {
            gap: 2rem;
        }

        .WhatsApp,
        .Facebook {
            padding: 1.5rem;
        }
    }

    /* 🔹 MÓVIL (768px y menos) */
    @media (max-width: 768px) {
        section {
            min-height: auto;
            padding: 1rem;
            gap: 1.5rem;
        }

        .title {
            font-size: 40px;
        }

        /* 🔹 Cambiar a layout vertical */
        article {
            grid-template-columns: 1fr;
            gap: 2rem;
        }

        .WhatsApp,
        .Facebook {
            padding: 1.5rem 1rem;
            margin: 0 auto;
            max-width: 400px;
            width: 100%;
        }

        .WhatsApp h2,
        .Facebook h2 {
            font-size: 30px;
        }

        .apptitle,
        .facetitle {
            gap: 15px;
            margin-bottom: 1rem;
        }

        img {
            height: 50px;
            width: 50px;
        }

        .WhatsApp span,
        .Facebook span {
            font-size: 20px;
            max-width: 100%;
            margin-bottom: 1.5rem;
        }

        .btnWhat,
        .btnFace {
            font-size: 22px;
            height: 60px;
            width: 200px;
        }
    }

    /* 🔹 MÓVIL PEQUEÑO (480px y menos) */
    @media (max-width: 480px) {
        section {
            padding: 0.5rem;
        }

        .title {
            font-size: 32px;
        }

        .WhatsApp,
        .Facebook {
            padding: 1rem;
            max-width: 100%;
        }

        .WhatsApp h2,
        .Facebook h2 {
            font-size: 26px;
        }

        .apptitle,
        .facetitle {
            flex-direction: column;
            gap: 10px;
            text-align: center;
        }

        img {
            height: 45px;
            width: 45px;
        }

        .WhatsApp span,
        .Facebook span {
            font-size: 18px;
            margin-bottom: 1rem;
        }

        .btnWhat,
        .btnFace {
            font-size: 20px;
            height: 55px;
            width: 180px;
        }
    }

    /* 🔹 MÓVIL MUY PEQUEÑO (360px y menos) */
    @media (max-width: 360px) {
        .title {
            font-size: 28px;
        }

        .WhatsApp h2,
        .Facebook h2 {
            font-size: 22px;
        }

        .WhatsApp span,
        .Facebook span {
            font-size: 16px;
        }

        .btnWhat,
        .btnFace {
            font-size: 18px;
            height: 50px;
            width: 160px;
        }
    }
</style>
